#!/usr/bin/env bash
set -euo pipefail
ROOT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"

usage() {
  echo "$0 <artist> <file...>" >&2
  exit 1
}

# shellcheck source=/dev/null
source "$ROOT_DIR/utils/source.sh"
source_modules \
  utils/db.sh \
  utils/profile.sh \
  utils/object.sh \
  utils/image.sh \
  utils/sequence.sh \
  utils/archive.sh

add_file() {
  local artist
  local input
  local mime
  local status
  artist=$1
  input=$2
  if [ ! -f "$input" ] || [ ! -r "$input" ]; then
    echo "input is not a readable file: $input" >&2
    return 1
  fi
  mime=$(file -b --mime-type -- "$input")
  case "$mime" in
    application/zip | application/x-zip | application/x-zip-compressed)
      archive_add "$artist" "$input"
      ;;
    *)
      if image_add "$artist" "$input"; then
        return 0
      else
        status=$?
      fi
      if [ "$status" -eq 2 ]; then
        return 0
      fi
      return "$status"
      ;;
  esac
}

main() {
  local artist
  local input
  local status
  [ "$#" -ge 2 ] || usage
  artist=$1
  shift
  profile_prepare
  for input in "$@"; do
    if add_file "$artist" "$input"; then
      continue
    else
      status=$?
    fi
    if [ "$status" -eq 2 ]; then
      continue
    fi
    return "$status"
  done
}

main "$@"
